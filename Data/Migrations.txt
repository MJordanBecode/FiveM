# Crée la migration initiale
dotnet ef migrations add InitialMigration --no-build --project Data --startup-project Data

# Supprime la DB
dotnet ef database drop --force --no-build --project Data --startup-project Data

# Recrée la DB avec la migration
dotnet ef database update --no-build --project Data --startup-project Data