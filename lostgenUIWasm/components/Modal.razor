<section>
    <div>
        <div>
            @* Element supp " x "*@
        </div>
        <div>
            <div> @*@header*@ </div>
            
            <div>@* @content *@</div>
        </div>
        <div>
            @*Footer*@
        </div>
    </div>
</section>

@code {
    //Pouvoir gérer la taille de la modal, pouvoir donner un nom à la modal, pouvoir mettre un système de boutton[voir un autre composant exprès]
    //Pouvoir gérer le style de la modal (ex: primary, secondary, etc.)
    //Pouvoir gérer un système de close pour la modal (ex: un boutton, un click sur le fond, etc.)

}
